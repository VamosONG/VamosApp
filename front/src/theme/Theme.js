import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    fonts: {
        heading: 'DIN Alternate Black, sans-serif',
        bold: 'DIN Alternate, sans-serif',
        medium: 'DIN Medium, sans-serif',
    },
});

export default theme;